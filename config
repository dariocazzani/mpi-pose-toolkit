# The data would be downloaded to this path
# Make sure you have approx 25GB space in this
# path to download the complete training set.
destination='./'

# Since you need realistic humans with head location data,
# I recommend downloading all subjects for a comprehensive dataset
subjects=(1 2 3 4 5 6 7 8)
# subjects=(1 2)

# Since you specifically need multiple views (requirement #3),
# enable the extra camera views
download_extra_wall_cameras=1
# Adding ceiling cameras will give you even more diverse viewpoints
download_extra_ceiling_cameras=1

# Keep the segmentation masks (useful for many computer vision tasks)
# download_masks=1

# Most importantly, set this to 1 to actually download the dataset
ready_to_download=1